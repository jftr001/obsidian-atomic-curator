{
	"name": "atomic-curator",
	"version": "0.1.0",
	"description": "Turn raw highlights and messy notes into clean atomic notes with AI, following Zettelkasten principles.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"zettelkasten",
		"atomic-notes",
		"ai",
		"pkm"
	],
	"author": "Juan Francisco",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.12.7",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.20.2",
		"obsidian": "latest",
		"tslib": "2.6.2",
		"typescript": "5.4.5"
	}
}
